export const enum ENVIRONMENT {
  development = 'development',
  production = 'production',
}

export const enum URLS {
  development = 'https://api.blockstream.space/testnet',
  production = 'https://api.blockstream.space',
}

export enum STATE {
  pending = 'pending',
  queued = 'queued',
  sent = 'sent',
}
